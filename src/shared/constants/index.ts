export enum FILE {
  RANDOM_NAME_KEY_LENGTH = 32,
  MAXIMUM_SIZE_IN_BYTES = 500 * 1024
}


export enum MILLISECOND {
  ONE_MINUTE = 60 * 1000
}

export enum LANGUAGE {
  EN = 'en',
  PT_BR = 'pt-BR',
}
